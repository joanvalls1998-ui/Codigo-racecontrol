name = "racecontrol-news-proxy"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Configura aquí tu dominio personalizado si lo tienes
# routes = [
#   { pattern = "racecontrol-news.joanvalls.workers.dev", custom_domain = true }
]

[vars]
ALLOWED_ORIGINS = "https://joanvalls1998-ui.github.io,http://localhost:3000"
